# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: foundryfriend
infra:
  provider: bicep
  path: ./infra
services:
  # Define application services here as the project grows
  # Example:
  # api:
  #   project: ./src/api
  #   language: py
  #   host: appservice
